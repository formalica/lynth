lets implement `lynth` tactic in lean which can be called against goals like `def f : {n:Nat // pred Nat} := by lynth` (f will be computable and we should instantiate n) types or `theorem thm: a+b=b+a := by lynth` propositions. underlaying architecture should be very similar to z3 with different procedures and if procedure fails then we it should return theory explanation(why it is failed or if not failed then we should add new information which inferred by given procedure when he tried to solve problem)  of why it is failed and it can be used by other/next procedure. but we should directly work with lean/mathlib terms and definitions. z3 have variable sharing mechanism but since lean is very expressive language we should include variable sharing as part of procedure explanation(return new information in generic way, maybe by returning new inferred `var_name=x` predicate(along with proof of predicate). everything should be proved so decision procedures should also reconstruct proof. no need to create separate theories because i do not like that hierarchy , try to create separate specialized procedures like in z3 and each procedure can have its own sub procedure and so on(such hierarchy is more natural) and each procedure can have its own internal theory/language/representation of problem and translate it from/to lean like in z3(but z3 have pure base languages and type system and pure general language, lean lang is more expressive and is more canonical). we will have no iterative or staged smt solving, user will just express what he want to solve via types and we should fill data and proof necessary propositions over data. 

download z3 and explore it, start to implement some basic thinks like sat solver procedure and then linear equation/inequality system solvers(see which algorithm z3 uses) then start to map various problems to this procedures, and continue to iteratively add new procedures and map new kind of problems into them. keep code clean and well structure procedures in separate dirs/files. since problem is very big then do not stop and just keep implementing new and new things without stopping as continues agent. since we need to reconstruct proofs then best way is to prove that procedure is correct via some theorem and then use that theorem in final reconstructed proof of users goal, in this case you can keep some recosnstuction theorem with sorry(or you can declare them as axiom) if they hard to prove(but you sure that they are correct) then we will prove them later. just write tests and in tests make sure that it depends only from lean native axioms and from yet not proved our internal theorems/axioms.

it is recomended to rebuild z3 with DZ3_ENABLE_TRACING_FOR_NON_DEBUG=ON flag to see its runtime traces and it can help to better and faster understand how z3 works

we should implement z3 theories but wrap them under procedures with same high level interface, and theory explaination and information sharing and type information enrichment will happen through that interface 

