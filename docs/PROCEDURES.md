# Procedure registry

Each procedure is a ported **theory** with its own internal language,
its own decision sub-procedures, and translation to/from Lean terms —
the hierarchy SPEC asks for (procedures owning sub-procedures, no
separate theory layer). Combination stays sequential explanation-passing.

| Procedure (theory) | Internal language | Sub-procedures / oracle | Reconstruction | Status |
|---|---|---|---|---|
| `Sat` (`Lynth/Sat`) | `CNF` (`Syntax.lean`), `PropForm` skeleton (`Encode.lean`) | Watched-literal CDCL core (`Watch.lean`, native CreuSAT port: 2WL + blockers + circular search, VMTF, phase saving, array state) + first-UIP learning with recorded resolution traces (`checkTrace`, fuzz-validated) + backjump + geometric restarts + `checkSat` gate on SAT answers (`Cdcl.lean`); DPLL reference (`Solver.lean`); Tseitin validity oracle over goal+hyps | oracle-guided `assumption/rfl/simp_all/grind/decide`; `lynth_sat_resolve` activates with trace validation | done (9×9 Sudoku + Star Battle scale pilots); EMA restarts + clause-DB reduction + DRAT TODO |
| `Arith` (`Lynth/Arith`) | `LinSys` (`Linear.lean`); `LeC` ℚ-systems (`Fourier.lean`) | FM elimination with Farkas lineage + validation (`checkCert`, fuzz-verified) + tableau Simplex à la Dutertre–de Moura + branch-and-bound for ℤ-completeness, differential-tested (`Fourier.solve`, `Simplex.solve`, `BranchBound.bb`); `Recognize` translates `Int`/`Nat` comparisons exactly (`+1` shifts for strict) | verified certs traced; kernel-checked `omega` closes | PROVED: `FarkasSound.farkas_sound` + `checkCert_sound` (native axioms, zero sorries); Simplex emits validated `DdMExplanation`s (lineage + blocking + residual, fuzz-checked); remaining: explanation soundness theorem, Lean-term denotes link, `lynth_farkas` activation |
| `EUF` (`Lynth/Euf`) | equality graph over `Eq` hyps (`Closure.lean` union-find) | BFS path search + congruence fixpoint (`congr` edges) + `Ne`/`False` close by contradiction | `Eq.trans`/`Eq.symm`/`congr` proof terms, kernel-checked, zero axioms | done; failure shares derived equalities into context (`shareDerived`) for later procedures |
| `Arrays` (extensional arrays) | `select`/`store` nodes over `Array` (`Rules.asSelect/asStore`) | read-over-write R1 (same index) + R2 (other index, `≠` premise) with core-lemma proofs (`Array.getElem_set_self/ne`); bound premises by context scan + `decide`; select-congruence via shared closure | core-lemma proof terms, kernel-checked | done (R1/R2 e2e; dependent-proof congruence falls through to `sat`) |
| `Datatypes` (inductive types) | constructor/discriminator terms over EUF graph | injectivity splintering + discrimination via core bounded `injections` fixpoint (plain `Eq`s forbidden); enrich-and-yield, closes on contradiction | core-tactic proofs, kernel-checked, zero axioms | done (Option/List/nested/discrimination; substitution behavior deferred) |
| `BV` (`Lynth/BV`) | reified `BvTerm` (const/var/and/or/xor/not/add) | Tseitin blast (ripple-carry adder) + CDCL validity oracles (`checkValidEq/Ne`, bidirectional fuzz vs brute force); `Recognize` translates `BitVec` goals | oracle traced; kernel-checked `bv_decide` closes | done (bitwise ops + add + eq/ne; comparisons TODO) |
| `Quant` (quantifiers) | trigger-indexed `∀`-hyps + EUF classes | E-matching ground instantiation: triggers matched against ground pool syntactically + modulo congruence (`Trigger.matchMod`, canon-validated); proven instances asserted (`Lynth/Quant/`); full MBQI blocked (open-term evaluation) | `hyp arg` instances, kernel-checked; procedure always yields | done (single/multi-premise, congruence-modulo, fallthrough tested) |
| `Ring` (`Lynth/Ring`) | semiring identities | kernel-checked `ring` normalizer (cf. Z3 `grobner`/`polynomial`) | `ring` | done |
| `Nlin` (`Lynth/Nlin`) | polynomial inequalities | kernel-checked `nlinarith` (Positivstellensatz-style products) | `nlinarith` | done |
| `Witness` (`Lynth/Witness`) | `Subtype` + `Exists` goals | `Nat`/`Int` enumeration (256) + `Bool` + diagonal `Prod` pairs + complete `Fin n` | explicit `Subtype.mk`/`Exists.intro` terms + `decide/rfl/omega/simp_all` side close | done for scalars, pairs, finite types; other domains TODO |
| `FinSearch` (`Lynth/FinSearch`) | `FTerm`/`FProp` over finite cardinalities (`Syntax.lean`): eq/ne/distinct/lt/le/boolean structure/`exactK` | detector + router (`Detect.lean`: cardinality estimate, named cell/CNF/fuel thresholds); recognizer for `Eq`/`Ne`/`LT`/`LE`/`And`/`Or`/`Not`/`Iff`/bounded-`∀`-unrolling/`List.Pairwise`/`Fintype.card`-subtype counting over `Fin n`/`Bool` (`Recognize.lean`); one-hot + Tseitin CNF encoder (`Encode.lean`); lazy CDCL(T) split for big pieces (opaque atoms + blocking-clause learning, `Theory.lean`); CDCL SAT child; closed value terms (nested `fun` + `List.getD` tables) decoded from models (`Procedure.lean`) | search untrusted, verdict proved: `decide`-verified models only (user predicates unfolded, current-module only); differential fuzz vs brute force (`Test/FinFuzz.lean`, 0 mismatches) | done (4×4 Sudoku + 4×4 Towers + 4×4 Star Battle + 9×9 Sudoku + 9×9 partial-Sudoku pilots, scalar routing to `Witness`) |
| `listsynth` (`Lynth/FinSearch/ListInfer`) | `List Nat` bounds: length window + value pool + reference domains | polymorphic bound inference over one total dispatch table (`inferenceRow`, unknown default): length/elem/target questions per Lean call, relation questions for non-`Eq` props (`Sublist`/`Perm` domains), peel questions for nested calls; definition-unfolding closed readers; strategies: sublist enumeration, sorted-first + bounded pool enumeration over length windows, widened-pool retry | enumeration untrusted, verdict proved: shared kernel-checked closer per candidate | done (take/drop, reverse/map chain, erase/replace, subset-sum, sort-perm pilots) |
| Combination model | sequential explanation-passing (iterative solving permitted inside a procedure, per SPEC) | EUF shares proven equalities; all procedures read the enriched context | — | done (this IS the combination mechanism) |
| `lynth_grind` / `lynth_grind?` (`Lynth/Grind`) | Lean core grind engine (e-graph + theories, reused natively) | tactic elaborates our syntax/config (`Tactic.lean`), then delegates to the core engine (`Lean.Elab.Tactic.evalGrindCore` / `evalGrindTraceCore`); `?`-suggestions rebuilt through our quotation so they read `lynth_grind` | core kernel-checked proofs | done (parity: EUF/arith/prop goals + `only`/params/`?` suggestions); ported engine copy (`Lynth/Grind`, 184 files) builds but is NOT executed — its C++-only `@[extern]` kernels (`preprocess`, `internalize`, …) have no native objects under `lake env lean`, so every goal crashed; interpreted-safe fixes applied along the way (`initialize` over `builtin_initialize`, plain `simproc_decl` over `builtin_*`, core `normExt` base set). Feeding `@[lynth_grind]` theorems as extra params, wiring as a `lynth` procedure, and upstream-test adaptation done (`Test/Grind/`: 100 upstream files adapted, all green). Feeding `@[lynth_grind]` theorems as extra params and wiring as a `lynth` procedure are TODO |

Reconstruction theorems live in `Lynth/Certificates.lean` as sound,
checker-conditioned statements (`lynth_sat_resolve`, `lynth_farkas`;
no `axiom`s, no `sorry`s anywhere). Witness reconstruction is direct
(`Subtype.mk`). `sorry` is banned.
